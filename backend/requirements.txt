fastapi==0.115.0
uvicorn[standard]==0.30.6
uvloop==0.21.0  # High-performance event loop for asyncio
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# persistence (optional but scaffolded)
SQLAlchemy==2.0.35
psycopg[binary]==3.2.2

# auth/util
PyJWT==2.9.0
httpx==0.27.2
requests==2.32.3
stripe==10.12.0
reportlab==3.6.13
lob==2023-08-01  # Physical mail delivery via Lob.com API

# tests
pytest==8.3.3
pytest-asyncio==0.24.0

# ============================================
# COORDINATION NOTE: Added by AI Assistant #1 (Auto)
# Working backwards through audit items - Issue #5: Rate Limiting
# ============================================
# Rate limiting middleware (AUDIT ITEM #5)
slowapi==0.1.9

# Error tracking and monitoring
sentry-sdk[fastapi]==2.19.0

# Email validation
email-validator
