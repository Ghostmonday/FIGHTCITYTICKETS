fastapi==0.115.5
uvicorn[standard]==0.30.6
uvloop==0.21.0  # High-performance event loop for asyncio
pydantic==2.10.1
pydantic-settings==2.6.1
python-dotenv==1.0.1

# persistence (optional but scaffolded)
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

# auth/util
PyJWT==2.10.1
httpx==0.28.1
requests==2.32.3
stripe==14.3.0
reportlab==4.2.5
lob==4.5.4  # Physical mail delivery via Lob.com API

# tests
pytest==8.3.4
pytest-asyncio==0.24.0

# ============================================
# COORDINATION NOTE: Added by AI Assistant #1 (Auto)
# Working backwards through audit items - Issue #5: Rate Limiting
# ============================================
# Rate limiting middleware (AUDIT ITEM #5)
slowapi==0.1.9

# Error tracking and monitoring
sentry-sdk[fastapi]==2.19.2

# Email validation
email-validator==2.2.0
