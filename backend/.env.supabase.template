# =============================================================================
# Supabase Configuration for FightCityTickets Backend
# =============================================================================
# Copy this file to .env.supabase and fill in the values
# NEVER commit .env.supabase to version control
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API

# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Service role key (has full access, bypasses RLS)
# WARNING: Never expose this in client-side code
SUPABASE_SERVICE_KEY=skey_your_service_role_key_here

# Anon key (public-safe, used by frontend)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your_anon_key_here

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (if using Supabase DB instead of PostgreSQL container)
# -----------------------------------------------------------------------------
# Get from Supabase Dashboard → Settings → Database → Connection string
# DATABASE_URL=postgresql://postgres:password@host:5432/postgres

# -----------------------------------------------------------------------------
# AUTH CONFIGURATION
# -----------------------------------------------------------------------------
# JWT secret for verifying Supabase auth tokens
# Get from: Supabase Dashboard → Settings → API → JWT Secret
SUPABASE_JWT_SECRET=your_jwt_secret_here

# =============================================================================
# RLS POLICIES ALREADY CONFIGURED:
# - profiles: SELECT/UPDATE own
# - tickets: SELECT/INSERT own
# - evidence: ALL own (WITH CHECK)
# - appeals: SELECT/INSERT/UPDATE own
# - storage.objects: INSERT/SELECT/DELETE own evidence files
# =============================================================================
