
# =============================================================================
# Supabase Configuration for FightCityTickets Frontend
# =============================================================================
# Copy this file to .env.local or add to Vercel Environment Variables
# NEVER commit actual values to version control
# ==============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Frontend-safe)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API

# Public URL - safe to expose in client code
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anonymous key - safe to expose in client code
# Used by Supabase JS client for authentication
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your_anon_key_here

# -----------------------------------------------------------------------------
# AUTH CONFIGURATION
# -----------------------------------------------------------------------------
# Enable/disable email confirmations during signup
NEXT_PUBLIC_SUPABASE_EMAIL_CONFIRMATIONS_ENABLED=false

# Redirect URL after email confirmation
NEXT_PUBLIC_SUPABASE_EMAIL_CONFIRM_URL=http://localhost:3000/welcome

# -----------------------------------------------------------------------------
# STORAGE CONFIGURATION
# -----------------------------------------------------------------------------
# Evidence bucket name (matches Supabase storage bucket)
NEXT_PUBLIC_SUPABASE_EVIDENCE_BUCKET=evidence

# Max upload size in bytes (10MB = 10485760)
NEXT_PUBLIC_SUPABASE_MAX_UPLOAD_SIZE=10485760

# Allowed MIME types for evidence uploads
NEXT_PUBLIC_SUPABASE_ALLOWED_MIME_TYPES=image/jpeg,image/png,image/heic,image/webp

# =============================================================================
# FRONTEND INTEGRATION NOTES:
# =============================================================================
# 1. Install Supabase JS client: npm install @supabase/supabase-js
#
# 2. Initialize client in your app:
#    import { createClient } from '@supabase/supabase-js'
#    const supabase = createClient(
#      process.env.NEXT_PUBLIC_SUPABASE_URL,
#      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
#    )
#
# 3. RLS policies ensure users can only access their own data
#    - User profiles: read/write own
#    - Tickets: create/read own
#    - Evidence: create/read/delete own
#    - Appeals: create/read own
#
# 4. For server-side operations (admin, system-wide queries),
#    use the service role key on the backend only
#    (stored in backend/.env.supabase.template, NOT here)
# =============================================================================
